'use strict';

var Base = require('../../bases/view');

module.exports = Base.extend({
    className: 'home google',
    wapAttributes: {
        cellpadding: 0
    }
});

module.exports.id = 'home/google';
