'use strict';

var Base = require('../../../../../common/app/bases/view').requireView('categories/list');

module.exports = Base.extend({
    id: 'list',
    className: 'list-view',
    postRender: function() {

    }
});
