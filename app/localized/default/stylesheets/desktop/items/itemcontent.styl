@import '../helpers/variables.styl';
@import '../helpers/mixins.styl';
@import '../helpers/functions.styl';

#item-content {
    box(#fff, #e4e4e4 , 3px, 0 20px);
    display: inline-block;
    width: 620px

    > h1 {
        font-size: 2.7rem;
        padding-right: 40px
    }
    .item-info {
        color: grey(40%)
    }
}