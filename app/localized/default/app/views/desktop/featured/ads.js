'use strict';

var Base = require('../../../../../common/app/bases/view').requireView('featured/ads');

module.exports = Base.extend({
    tagName: 'main',
    id: 'featured-ads-view',
    className: 'featured-ads-view'
});
